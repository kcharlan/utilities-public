[pytest]
markers =
    e2e: End-to-end browser tests (require Playwright)
    allows_http_error(status, count, endpoint_pattern): exact Chromium console errors and matching HTTP responses from deliberately exercised failures
