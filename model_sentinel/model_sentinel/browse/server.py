from __future__ import annotations

from typing import Any


def run_browse(
    *,
    db: Any,
    loaded: Any,
    port: int,
    open_browser: bool,
    initial_provider: str | None,
) -> int:
    """Task-1 placeholder for the browser server implemented in a later task."""
    return 0
