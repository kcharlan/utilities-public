# Temporary security bridge: MoviePy 2.2.1 requires vulnerable Pillow <12.
# This audited upstream commit removes that bound; return to a stable release
# once MoviePy publishes one with Pillow 12 support.
moviepy @ git+https://github.com/Zulko/moviepy.git@211e4b15f6ce4f34a6a9efbfff40590e43a68f77
pillow==12.3.0
numpy==2.5.3
tqdm==4.70.1
